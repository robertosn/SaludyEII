import React from "react"
import { Link } from "gatsby"

const Logo = (props) => (
  <div className="site-logo">
    document.getElementById("ID_del_div").innerHTML='<img src="/assets/logo2.jpg" />';
  </div>
)

export default Logo